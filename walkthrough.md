# アプリ改修ガイド (Walkthrough)

## 概要
本改修では、`implementation plan.md` に基づき、時系列データの分析機能を追加しました。
具体的には、過去の全CSVデータを一括処理し、**業種**および**時価総額帯**ごとの騰落率や売買代金モメンタム（5日/20日平均比率など）の推移を可視化できるようにしました。

## 変更点詳細

### 1. データ処理ロジックの変更 (`3-data_processor_v01.py`)

*   **全件ループ処理の実装**:
    *   従来は「最新の1ファイル」のみを処理していましたが、`data/raw/japan_all_stock` 内の全CSVファイルを日付順に処理するループ構造に変更しました。
    *   これにより、過去すべての日付について `sector_summary` および `momentum_summary` が生成されます。

*   **時価総額帯モメンタムの追加**:
    *   `momentum_summary` の生成ロジックを拡張し、従来の「業種別」に加え、「時価総額帯別（小型・中型・大型・超大型）」の売買代金集計も行い、移動平均（3, 5, 10, 20日）を算出するようにしました。
    *   CSV出力に `区分` カラムを追加し、`業種` または `時価総額帯` でデータを識別できるようにしました。

### 2. アプリケーションの機能追加 (`app.py`)

*   **Momentum Trends (新タブ)**:
    *   サイドバーに「Momentum Trends」を追加しました。
    *   ここでは、生成された全期間の `momentum_summary` データを読み込み、時系列チャートを表示します。
    *   **機能**:
        *   **Analysis Type**: 分析対象を「業種」か「時価総額帯」から選択できます。
        *   **Select Targets**: 比較したい業種やサイズ（例: 「電気機器」と「情報・通信業」、あるいは「小型」と「大型」など）を複数選択できます。
        *   **Line Chart**: 売買代金の「5日移動平均 / 20日移動平均」の比率推移を表示します。1.0を超えると短期的に活況であることを示します。
        *   **Heatmap**: 選択した対象のモメンタムの強弱を色で一覧表示します。

*   **データ読み込みの最適化**:
    *   `@st.cache_data` を使用して、大量の過去CSVファイルの読み込みをキャッシュし、パフォーマンスを維持しています。

## 操作手順

1.  **データ更新**:
    *   新しい生データCSV (`data/raw/...`) を配置した後、手動で `3-data_processor_v01.py` を実行するか、アプリ内の「Data Management」タブから更新処理を実行してください。これにより過去分も含めて再計算されます。

2.  **分析**:
    *   アプリを起動し、「Momentum Trends」タブを開きます。
    *   「Analysis Type」で「時価総額帯」を選び、「Select Targets」で「小型」「大型」などを選ぶと、サイズ別の資金流入の勢いの違いが時系列で確認できます。

## 技術的補足
*   **ファイル互換性**: 既存の `sector_summary` などのフォーマットは維持しつつ拡張しているため、既存のダッシュボード機能への影響は最小限に抑えています。
*   **拡張性**: 今後、新たな指標（RSIなど）を追加する場合も、`3-data_processor_v01.py` の集計ロジックに追加するだけで、同様の仕組みで可視化可能です。
